package ai;
import ai.Node;
import  transport.*;

public class Main {
    public static void main(String[] args) {
       Node n = new Node(null,null,null,null,null);
        System.out.println(n.successors());
    }
}
